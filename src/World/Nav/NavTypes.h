#pragma once

#include "System/EnginePrerequisites.h"

namespace AV{
    typedef uint8 NavMeshId;
    typedef uint16 NavQueryId;

    static const NavMeshId INVALID_NAV_MESH = 0xFF;
    static const NavQueryId INVALID_NAV_QUERY = 0xFFFF;
}
